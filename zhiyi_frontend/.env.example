# 智医 (ZhiYi) 前端环境变量
# 复制本文件为 .env 后生效

# 是否使用本地 Mock 数据（true=不连接后端，false=连接真实后端）
VITE_USE_MOCK=false

# 后端 API 基地址
# Docker 部署时使用 http://localhost:8002/api
# 本地直接启动后端（uvicorn 8000 端口）时使用 http://localhost:8000/api
VITE_API_BASE_URL=http://localhost:8002/api
