# 智医项目 · 前后端接口对接检查报告

> 检查时间：2026-08-06  
> 后端：FastAPI（8 个路由模块，65+ 端点）  
> 前端：Vue 3 + TypeScript（14 个视图组件，30+ API 调用）

---

> ⚠️ 状态说明：本文档是 2026-08-06 的历史检查快照，其中多数问题已在后续迭代中修复（管理员药品/检查项目增删改、医生新增/删除、购物车管理、检查报告录入、前端 WebSocket 物流追踪等）。当前实现请以根目录 `README.md` 和 Swagger `/docs` 为准。

---

## 一、前端调了但后端不存在的路由（会导致 404，必须修复）

### 1.1 `PUT /api/admin/drugs/{id}/toggle`

- **前端调用位置**：`zhiyi_frontend/src/views/admin/CatalogManagementView.vue` 第 32-33 行
- **前端代码**：`fetch(\`${apiBase}/${path}\`, { method: 'PUT' })`，其中 `path = admin/drugs/${item.id}/toggle`
- **后端实际情况**：没有 `/toggle` 子路由。后端药品上架/下架的方式是：
  - `PUT /api/admin/drugs/{drug_id}` — 更新药品（body 含 `is_active` 字段）
  - `DELETE /api/admin/drugs/{drug_id}` — 下架药品（设 `is_active=False`）
- **修复方案**：
  - 方案 A（改前端）：把 `/toggle` 改为 `PUT /api/admin/drugs/${item.id}`，body 传 `{ is_active: !item.is_active }`
  - 方案 B（改后端）：在 `admin.py` 增加 `PUT /api/admin/drugs/{drug_id}/toggle` 路由

### 1.2 `PUT /api/admin/exam-items/{id}/toggle`

- **前端调用位置**：`zhiyi_frontend/src/views/admin/CatalogManagementView.vue` 第 32-33 行
- **后端实际情况**：同样没有 `/toggle` 子路由。后端方式是：
  - `PUT /api/admin/exam-items/{item_id}` — 更新检查项目（body 含 `is_active`）
  - `DELETE /api/admin/exam-items/{item_id}` — 下架检查项目
- **修复方案**：同上，改前端路径或后端加路由

---

## 二、字段名 / 响应结构不匹配（会导致 422 或 UI 异常，应修复）

### 2.1 `POST /api/diagnosis/qa` — 请求字段名错误

- **前端调用位置**：`zhiyi_frontend/src/views/doctor/KnowledgeBaseView.vue` 第 29 行
- **前端发送**：`{ patient_id: 1, symptoms: q }`
- **后端期望**（`DiagnosisQARequest` schema）：`{ question: str, patient_id?: int }`
- **问题**：前端用 `symptoms` 字段名，后端要求 `question`，导致 Pydantic 422 验证错误
- **修复**：前端把 `symptoms: q` 改为 `question: q`

### 2.2 `GET /api/admin/orders` — 响应结构不匹配

- **前端调用位置**：
  - `zhiyi_frontend/src/views/admin/OrderShipmentView.vue` — 期望 `items: Array<{ drug_name, quantity }>`
  - `zhiyi_frontend/src/views/doctor/PrescriptionReviewView.vue` — 同样期望 `items` 数组
- **后端实际返回**（`admin.py` 第 328-337 行）：
  ```json
  {
    "id": 1,
    "order_no": "DRUG20260806...",
    "items_summary": "硝苯地平缓释片×2, 阿莫西林胶囊×1",
    "total_price": 98.5,
    "pay_status": "paid",
    "delivery_status": "pending",
    "address": "...",
    "created_at": "2026-08-06T..."
  }
  ```
- **问题**：后端返回 `items_summary`（逗号分隔字符串），前端期望 `items`（对象数组），导致前端 `v-for="it in (o.items || [])"` 渲染不出订单项
- **修复方案**：
  - 方案 A（改后端）：在 `admin_list_orders` 中把 `items` 数组也加入响应
  - 方案 B（改前端）：解析 `items_summary` 字符串为数组

---

## 三、后端已实现但前端未调用的接口（28 个，按功能分组）

### 认证模块 `/api/auth`（2 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `POST /api/auth/logout` | 用户登出（Token 加入黑名单） | 前端退出登录只清 localStorage，未调后端失效 Token |
| `GET /api/auth/me` | 获取当前用户信息 | 前端依赖 localStorage 缓存，刷新后可能不同步 |

### 诊断模块 `/api/diagnosis`（1 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `GET /api/diagnosis/{id}` | 查看诊断详情 | 前端只有列表，无详情查看入口 |

### 检查模块 `/api/exams`（6 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `GET /api/exams/items/{id}` | 检查项目详情 | 前端列表无点击查看详情 |
| `PUT /api/exams/cart/{item_id}` | 更新检查购物车数量 | 购物车中无法改数量 |
| `DELETE /api/exams/cart/{item_id}` | 移出检查购物车 | 购物车中无法删除单项 |
| `DELETE /api/exams/cart` | 清空检查购物车 | 无一键清空功能 |
| `POST /api/exams/orders/{id}/cancel` | 取消预约 | 患者无法取消已下单的检查预约 |
| `PUT /api/exams/appointments/{id}/report` | 上传检查报告 | 医生/管理员无法上传报告 |
| `GET /api/exams/appointments/{id}/interpret` | AI 解读检查报告 | 无独立 AI 解读入口 |

### 药品模块 `/api/drugs`（3 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `GET /api/drugs/items/{id}` | 药品详情 | 前端无药品详情页 |
| `GET /api/drugs/orders/{id}` | 订单详情 | 前端无药品订单详情页 |
| `DELETE /api/drugs/cart` | 清空药品购物车 | 无一键清空功能 |

### 物流模块 `/api/logistics`（3 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `GET /api/logistics/{order_id}` | HTTP 查询物流状态 | 前端用 `/api/drugs/orders/{id}/status` 代替 |
| `WS /api/logistics/ws/{order_id}` | WebSocket 实时位置推送 | **完全未实现前端 WebSocket 连接**，物流地图无实时更新 |
| `POST /api/logistics/{order_id}/ship` | 物流路由发货 | 前端用的是 `/api/admin/orders/{id}/ship`（功能重复） |

### 健康档案模块 `/api/profile`（2 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `GET /api/profile/{id}/visits` | 就诊记录 | 前端未单独调用（档案接口内嵌了 recent_visits） |
| `GET /api/profile/{id}/trend` | 健康趋势 | 同上（档案接口内嵌了 health_trend） |

### 管理后台模块 `/api/admin`（9 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `POST /api/admin/doctors` | 创建医生账号 | 管理端无创建医生表单 |
| `DELETE /api/admin/doctors/{id}` | 删除医生 | 管理端只有启用/禁用，无删除 |
| `POST /api/admin/drugs` | 新增药品 | 管理端无新增药品表单 |
| `PUT /api/admin/drugs/{id}` | 更新药品 | 被前端错误调用为 `/toggle` |
| `DELETE /api/admin/drugs/{id}` | 下架药品 | 前端用 toggle 代替 |
| `POST /api/admin/exam-items` | 新增检查项目 | 管理端无新增表单 |
| `PUT /api/admin/exam-items/{id}` | 更新检查项目 | 同上 |
| `DELETE /api/admin/exam-items/{id}` | 下架检查项目 | 同上 |
| `GET /api/admin/overview` | 管理概览（兼容旧接口） | 前端用的是 `/dashboard` |

### 支付模块 `/api/pay`（2 个）
| 端点 | 说明 | 影响 |
|------|------|------|
| `POST /api/pay/alipay/create` | 创建支付宝支付 | 前端 pay.html 可能调用，但未在 Vue 组件中直接对接 |
| `POST /api/pay/alipay/notify` | 支付宝异步回调 | 服务端回调，前端不需要调用（正常） |

---

## 四、修复优先级建议

### P0 — 必须修复（会导致功能完全不可用）
1. **CatalogManagementView 的 `/toggle` 路由**：药品/检查上下架功能完全不可用
2. **KnowledgeBaseView 的字段名错误**：医学知识库问答功能完全不可用

### P1 — 应修复（会导致 UI 显示异常）
3. **admin/orders 响应结构**：订单发货和处方审核页面看不到订单项明细
4. **WebSocket 物流追踪**：前端未实现 WS 连接，物流地图无实时位置更新

### P2 — 建议补充（功能完善）
5. 认证模块：登出调后端、刷新时调 `/auth/me` 同步用户信息
6. 检查/药品：购物车单项删除/数量修改/清空、取消预约
7. 管理后台：新增/删除医生、新增/编辑/删除药品和检查项目
8. 检查报告：上传报告、AI 解读入口
